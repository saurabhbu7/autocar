describe('User Management System', () => { beforeEach(() => { cy.visit('index.html'); }); it('Adds a new user', () => { cy.get('#name').type('New User'); cy.get('#email').type('newuser@example.com'); cy.get('button[type="submit"]').click(); cy.get('#userList').contains('New User'); }); it('Edits an existing user', () => { cy.get('.edit').first().click(); cy.get('#name').clear().type('Edited User'); cy.get('#email').clear().type('editeduser@example.com'); cy.get('button[type="submit"]').click(); cy.get('#userList').contains('Edited User'); }); it('Deletes a user', () => { cy.get('.delete').first().click(); cy.get('#userList').should('not.contain', 'Edited User'); }); });